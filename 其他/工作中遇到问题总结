问题1：
  在命名视图中如何加入嵌套路由？

问题2：
  路由的过渡动画？出场动画，入场动画

问题3：
  删除组件在左滑时，如何保证不向下滑动？

问题4：
  怎么区别HTML标签的属性和特性？

问题5：
  const instance = axios.create({
    baseURL:'',
    timeout: 15000,  //请求超时的毫秒数
    // withCredentials: true,   //跨域请求时是否需要使用凭证
    headers: {"Content-Type": "application/json;charset=utf-8"}
})
会产生跨域问题，其原因就是withCredentials属性值为true引起。

问题6：
  如何实现数字滚动效果？
  1.countup.js

问题7：
  如何实现SSR(服务端渲染)?
  vue之nuxt

问题8：
  如何实现弹出框的入场，出场动画

问题9：
  如何在控制台，打印出不一样的字体（如：加个背景色啥的）
  console.log(chalk.white.bold.bgRed(`注意：当前是${env}环境               `))

问题10：
  JsBridge

问题11：
  在Vue中实现拖拽功能v-drag