1. endsWith()返回布尔值 ，表示参数字符串是否在源字符串的尾部

例如：
  var str = 'abcd'

  str.endsWith('d') // true


2. fixed()方法的返回值为string类型，其返回值形如：

<tt>StringObject</tt>

例如：
  str = "腾讯QQ";

  document.writeln( str.fixed() ); // <tt>腾讯QQ</tt>

3.includes()返回布尔值，表示是否找到了参数字符串

例如：
  var str = 'abcd'

  str.includes('c')

4. italics()方法用于把字符串显示为斜体，返回加了<i>标签的字符串

  如下所示:

  <i>string</i>

5. localeCompare(target) 返回值
说明比较结果的数字。如果 stringObject 小于 target，则 localeCompare() 返回小于 0 的数。如果 stringObject 大于 target，则该方法返回大于 0 的数。如果两个字符串相等，或根据本地排序规则没有区别，该方法返回 0。