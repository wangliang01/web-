在vue开发中，不要在ajax方法中直接用路由信息

例如：

ajax({
  data: {
    id: this.$route.query.id
  }
})

这种写法，有时在页面快速回退时，可能出现找不到id的情况，具体原因是：

当页面回退到此页面时，ajax因为是异步方法，所以，在ajax方法还没有执行，页面又回退到了上一个页面，这时，ajax()方法执行，然后去读取路由信息中的id,发现没有找到，这时接口就会报错。